model Project {
  project_id          Int     @id @default(autoincrement())
  name      String
  body      String
  user_id   String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}